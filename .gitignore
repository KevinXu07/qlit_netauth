# 抓包文件：内含可被直接解出的账号与密码密文，以及个人信息。
# 这既是隐私问题也是安全问题，务必不要提交。
*.har

# 本机的临时配置（测试时用的明文配置，可能含真实密码）
*.local.conf
test.conf

# 构建产物。
# 注意：这些模式必须锚定到仓库根目录（前导斜杠）。写成 "bin/" 会匹配任意层级
# 名为 bin 的目录，从而把 files/usr/bin/ 一并排除 —— 那里面是主程序。
/bin/
/build_dir/
/staging_dir/
/dl/
/out/
/sdk/
*.ipk

# 编辑器与系统文件
.DS_Store
*.swp
*~
